---
variables:
#  CI_DEBUG_TRACE: 'true'

stages:
  - test

test:
  script:
    - git clone https://gitlab-ci-token:${CI_JOB_TOKEN}@gitlab.com/kontur-private/platform/event-api.git/
    - ls -lAR event-api

