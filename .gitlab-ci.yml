---
variables:
#  CI_DEBUG_TRACE: 'true'

stages:
  - test

test:
  script:
    - set
    - pwd
    - git clone https://gitlab-ci-token:${CI_JOB_TOKEN}@gitlab.com/kontur-private/platform/event-api-tests.git/
    - ls -lAR event-api-tests

